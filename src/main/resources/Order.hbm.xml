<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE hibernate-mapping PUBLIC
        "-//Hibernate/Hibernate Mapping DTD 3.0//EN"
        "http://hibernate.sourceforge.net/hibernate-mapping-3.0.dtd">

<hibernate-mapping package="com.qs.entity">
    <!-- 上面package属性指定包名，这里name属性值对应类无需写全路径 -->
    <class name="Order" table="t_order">
        <id name="orderId" type="string" column="ORDER_ID" length="32">
            <!-- 配置之间生成策略为uuid -->
            <generator class="uuid"/>
        </id>

        <!-- 直接使用property属性设置 -->
        <property name="orderNumber" type="string">
            <column name="ORDER_NUMBER" length="15" not-null="true"/>
        </property>


        <many-to-one name="customer" column="CUSTOMER_ID" class="Customer"/>
    </class>
</hibernate-mapping>